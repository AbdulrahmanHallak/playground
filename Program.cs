namespace test;

partial class Program
{
    static void Main(string[] args)
    {
        SectionTitle("* Changing Terminal Color");

        // WriteXmlStream();

        // Compress();
        // Compress("brotli");
        // ArchiveContent();

        Encode();
    }
}
